// 倒树：叶子 = 更简单 / 先修；父节点 = 晋级。同层多个孩子 = 都要会（无序）。
(function () {
  const L = (name, m, kids) => {
    const n = { name, leaf: true, ...m };
    if (kids) n.children = [].concat(kids);
    return n;
  };
  const N = (name, m, kids) => {
    const n = { name, ...m };
    if (kids) n.children = [].concat(kids);
    return n;
  };
  const t = (volume, unit, topic) => ({ volume, unit, topic });
  const u15 = (topic) => t("上册", "5以内数的认识和加、减法", topic);
  const u610 = (topic) => t("上册", "6～10的认识和加、减法", topic);
  const u1120 = (topic) => t("上册", "11～20的认识", topic);
  const uCarry = (topic) => t("上册", "20以内的进位加法", topic);
  const uBorrow = (topic) => t("下册", "20以内的退位减法", topic);
  const u100n = (topic) => t("下册", "100以内数的认识", topic);
  const u100m = (topic) => t("下册", "100以内的口算加、减法", topic);
  const u100w = (topic) => t("下册", "100以内的笔算加、减法", topic);
  const uRel = (topic) => t("下册", "数量间的加减关系", topic);
  const uSolid = (topic) => t("上册", "认识立体图形", topic);
  const uPlane = (topic) => t("下册", "认识平面图形", topic);

  const recognize15 = N("1～5的认识", u15("1～5的认识"), [
    L("认、读、写1～5各数", u15("1～5的认识")),
    L("用数表示物体个数", u15("1～5的认识")),
    L("规范书写数字", u15("1～5的认识"))
  ]);

  const compare15 = N("比大小", u15("比大小"), [
    L("认识 >、<、=", u15("比大小")),
    L("用符号比较1～5各数的大小", u15("比大小")),
    L("用语言描述大小关系", u15("比大小")),
    recognize15
  ]);

  const ordinal15 = N("第几和几个", u15("第几和几个"), [
    L("区分基数（几个）与序数（第几）", u15("第几和几个")),
    L("理解排列顺序", u15("第几和几个")),
    compare15
  ]);

  const compose5 = N("分与合", u15("分与合"), [
    L("2～5各数的组成", u15("分与合")),
    L("根据组成计算加减法", u15("分与合")),
    ordinal15
  ]);

  const add5 = N("加法的认识与计算", u15("加法的认识与计算"), [
    L("理解加法含义（合并）", u15("加法的认识与计算")),
    L("5以内加法口算", u15("加法的认识与计算")),
    L("看图列加法算式", u15("加法的认识与计算")),
    compose5
  ]);

  const sub5 = N("减法的认识与计算", u15("减法的认识与计算"), [
    L("理解减法含义（去掉一部分）", u15("减法的认识与计算")),
    L("5以内减法口算", u15("减法的认识与计算")),
    L("看图列减法算式", u15("减法的认识与计算")),
    add5
  ]);

  const zero5 = N("0的认识和加减法", u15("0的认识和加减法"), [
    L("0表示没有或起点", u15("0的认识和加减法")),
    L("一个数加0或减0仍得原数", u15("0的认识和加减法")),
    L("相同两数相减得0", u15("0的认识和加减法")),
    sub5
  ]);

  const recognize610 = N("6～10的认识", u610("6～10的认识"), [
    L("认、读、写6～10各数", u610("6～10的认识")),
    L("0～10的数序（正数、倒数）", u610("6～10的认识")),
    zero5
  ]);

  const ten610 = N("10的认识", u610("10的认识"), [
    L("10是最小的两位数", u610("10的认识")),
    L("10是由10个一组成的", u610("10的认识")),
    L("10的读写", u610("10的认识")),
    recognize610
  ]);

  const compare610 = N("比大小、第几", u610("比大小、第几"), [
    L("一一对应法比较多少", u610("比大小、第几")),
    L("用 >、<、= 比较6～10各数", u610("比大小、第几")),
    L("第几与几个", u610("比大小、第几")),
    ten610
  ]);

  const compose610 = N("6～10的分与合", u610("6～10的分与合"), [
    L("各数的组成", u610("6～10的分与合")),
    L("根据分与合计算", u610("6～10的分与合")),
    compare610
  ]);

  const calc610 = N("6～10的加法和减法", u610("6～10的加法和减法"), [
    L("根据数的组成计算", u610("6～10的加法和减法")),
    L("想加法算减法", u610("6～10的加法和减法")),
    L("一图四式", u610("6～10的加法和减法")),
    L("求总数用加法", u610("6～10的加法和减法")),
    L("求部分用减法", u610("6～10的加法和减法")),
    compose610
  ]);

  const mix610 = N("连加、连减与加减混合", u610("连加、连减与加减混合"), [
    L("连加计算", u610("连加、连减与加减混合")),
    L("连减计算", u610("连加、连减与加减混合")),
    L("加减混合（从左到右）", u610("连加、连减与加减混合")),
    calc610
  ]);

  const tenAgain = N("10的再认识", u1120("10的再认识"), [
    L("10个一是1个十", u1120("10的再认识")),
    L("认识计数单位“一”和“十”", u1120("10的再认识")),
    L("10个为一组数数", u1120("10的再认识")),
    mix610
  ]);

  const know1120 = N("11～20的认识", u1120("11～20的认识"), [
    L("认、读、写11～20各数", u1120("11～20的认识")),
    L("十几由1个十和几个一组成", u1120("11～20的认识")),
    L("20由2个十组成", u1120("11～20的认识")),
    tenAgain
  ]);

  const place20 = N("数位", u1120("数位"), [
    L("认识个位和十位", u1120("数位")),
    L("从高位写数、读数", u1120("数位")),
    L("个位没有计数单位写0占位", u1120("数位")),
    know1120
  ]);

  const order20 = N("数的顺序与比大小", u1120("数的顺序与比大小"), [
    L("11～20的数序", u1120("数的顺序与比大小")),
    L("按顺序比较大小", u1120("数的顺序与比大小")),
    L("利用数的组成比较", u1120("数的顺序与比大小")),
    place20
  ]);

  const noCarry20 = N("20以内的不进位加、减法", u1120("20以内的不进位加、减法"), [
    L("10加几及相应减法", u1120("20以内的不进位加、减法")),
    L("十几加几及相应减法", u1120("20以内的不进位加、减法")),
    L("20以内连加、连减", u1120("20以内的不进位加、减法")),
    L("加减混合运算", u1120("20以内的不进位加、减法")),
    order20
  ]);

  const names20 = N("加减法各部分名称", u1120("加减法各部分名称"), [
    L("加数、和", u1120("加减法各部分名称")),
    L("被减数、减数、差", u1120("加减法各部分名称")),
    noCarry20
  ]);

  const add9 = N("9加几", uCarry("9加几"), [
    L("凑十法计算", uCarry("9加几")),
    L("拆小数凑大数", uCarry("9加几")),
    names20
  ]);

  const add876 = N("8、7、6加几", uCarry("8、7、6加几"), [
    L("凑十法", uCarry("8、7、6加几")),
    L("拆大数凑小数", uCarry("8、7、6加几")),
    add9
  ]);

  const add5432 = N("5、4、3、2加几", uCarry("5、4、3、2加几"), [
    L("凑十法", uCarry("5、4、3、2加几")),
    L("交换加数位置和不变", uCarry("5、4、3、2加几")),
    add876
  ]);

  const carryAdd = N("进位加法应用", uCarry("解决问题"), [
    L("多角度求总数", uCarry("解决问题")),
    L("求原来共有多少（用掉+剩下）", uCarry("解决问题")),
    L("看图列式", uCarry("解决问题")),
    add5432
  ]);

  const sub9 = N("十几减9", uBorrow("十几减9"), [
    L("破十法", uBorrow("十几减9")),
    L("想加算减", uBorrow("十几减9")),
    L("连减法", uBorrow("十几减9")),
    carryAdd
  ]);

  const sub876 = N("十几减8、7、6", uBorrow("十几减8、7、6"), [
    L("破十法", uBorrow("十几减8、7、6")),
    L("想加算减", uBorrow("十几减8、7、6")),
    L("一图两式", uBorrow("十几减8、7、6")),
    sub9
  ]);

  const sub5432 = N("十几减5、4、3、2", uBorrow("十几减5、4、3、2"), [
    L("破十法", uBorrow("十几减5、4、3、2")),
    L("想加算减", uBorrow("十几减5、4、3、2")),
    L("20以内退位减法口算", uBorrow("十几减5、4、3、2")),
    sub876
  ]);

  const borrowSub = N("退位减法应用", uBorrow("解决问题"), [
    L("选择有效信息、排除干扰", uBorrow("解决问题")),
    L("求一个数比另一个数多（少）几", uBorrow("解决问题")),
    L("看图列式计算", uBorrow("解决问题")),
    sub5432
  ]);

  const count100 = N("数数与数的组成", u100n("数数与数的组成"), [
    L("一一对应数数", u100n("数数与数的组成")),
    L("按群计数（2个、5个、10个一组）", u100n("数数与数的组成")),
    L("10个一是1个十", u100n("数数与数的组成")),
    L("10个十是1个百", u100n("数数与数的组成")),
    borrowSub
  ]);

  const rw100 = N("读数与写数", u100n("读数与写数"), [
    L("从高位读起", u100n("读数与写数")),
    L("从高位写起", u100n("读数与写数")),
    L("个位、十位、百位", u100n("读数与写数")),
    L("空位写0占位", u100n("读数与写数")),
    count100
  ]);

  const order100 = N("数的顺序", u100n("数的顺序"), [
    L("100以内数的排列", u100n("数的顺序")),
    L("按规律填数", u100n("数的顺序")),
    rw100
  ]);

  const cmp100 = N("比较大小", u100n("比较大小"), [
    L("先比十位再比个位", u100n("比较大小")),
    L("用 >、<、= 比较100以内数", u100n("比较大小")),
    order100
  ]);

  const tensOne = N("整十数加一位数及相应减法", u100n("整十数加一位数及相应减法"), [
    L("利用数的组成计算", u100n("整十数加一位数及相应减法")),
    L("摆一摆，想一想", u100n("整十数加一位数及相应减法")),
    cmp100
  ]);

  const mentalAdd = N("口算加法", u100m("口算加法"), [
    L("整十数加整十数", u100m("口算加法")),
    L("两位数加一位数（不进位）", u100m("口算加法")),
    L("两位数加整十数", u100m("口算加法")),
    L("两位数加一位数（进位）", u100m("口算加法")),
    tensOne
  ]);

  const mentalSub = N("口算减法", u100m("口算减法"), [
    L("整十数减整十数", u100m("口算减法")),
    L("两位数减一位数（不退位）", u100m("口算减法")),
    L("两位数减整十数", u100m("口算减法")),
    L("两位数减一位数（退位）", u100m("口算减法")),
    mentalAdd
  ]);

  const mental100 = N("口算应用", u100m("解决问题"), [
    L("分析数量关系", u100m("解决问题")),
    L("列式计算并口答", u100m("解决问题")),
    mentalSub
  ]);

  const vertical = N("竖式计算", u100w("竖式计算"), [
    L("认识竖式格式", u100w("竖式计算")),
    L("相同数位对齐", u100w("竖式计算")),
    L("从个位算起", u100w("竖式计算")),
    mental100
  ]);

  const writtenAdd = N("笔算加法", u100w("笔算加法"), [
    L("两位数加两位数（不进位）", u100w("笔算加法")),
    L("两位数加两位数（进位）", u100w("笔算加法")),
    vertical
  ]);

  const writtenSub = N("笔算减法", u100w("笔算减法"), [
    L("两位数减两位数（不退位）", u100w("笔算减法")),
    L("两位数减两位数（退位）", u100w("笔算减法")),
    writtenAdd
  ]);

  const rel = N("数量关系分析", uRel("数量关系分析"), [
    L("理解问题中的数量关系", uRel("数量关系分析")),
    L("确定用加法还是减法", uRel("数量关系分析")),
    writtenSub
  ]);

  const apply100 = N("解决实际问题", uRel("解决实际问题"), [
    L("一步计算的加减应用题", uRel("解决实际问题")),
    L("画图或列表辅助分析", uRel("解决实际问题")),
    rel
  ]);

  const shapes3d = N("常见立体图形", uSolid("常见立体图形"), [
    L("长方体", uSolid("常见立体图形")),
    L("正方体", uSolid("常见立体图形")),
    L("圆柱", uSolid("常见立体图形")),
    L("球", uSolid("常见立体图形"))
  ]);

  const solid = N("辨认与分类", uSolid("辨认与分类"), [
    L("辨认立体图形", uSolid("辨认与分类")),
    L("按形状分类", uSolid("辨认与分类")),
    L("感受图形特征", uSolid("辨认与分类")),
    shapes3d
  ]);

  const shapes2d = N("平面图形的认识", uPlane("平面图形的认识"), [
    L("长方形", uPlane("平面图形的认识")),
    L("正方形", uPlane("平面图形的认识")),
    L("平行四边形", uPlane("平面图形的认识")),
    L("三角形", uPlane("平面图形的认识")),
    L("圆", uPlane("平面图形的认识")),
    L("从立体图形描（画、印、拓）出平面图形", uPlane("平面图形的认识"), solid)
  ]);

  const planeSort = N("平面图形的分类", uPlane("平面图形的分类"), [
    L("按形状分类", uPlane("平面图形的分类")),
    L("辨认和区分平面图形", uPlane("平面图形的分类")),
    shapes2d
  ]);

  const plane = N("平面图形的拼组", uPlane("平面图形的拼组"), [
    L("用同样图形拼组", uPlane("平面图形的拼组")),
    L("感受图形之间的关系", uPlane("平面图形的拼组")),
    L("七巧板拼指定图形", uPlane("平面图形的拼组")),
    planeSort
  ]);

  window.GRADE1_MATH_PRIORITY_TREE = {
    edition: "人教版（2024）",
    grade: 1,
    subject: "数学",
    tree: {
      name: "一年级数学",
      children: [
        { name: "数量", children: [apply100] },
        { name: "几何", children: [plane] }
      ]
    }
  };
})();
